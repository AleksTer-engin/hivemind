#!/usr/bin/env bash
# init-astro-docs.sh — инициализация документации Астролябии для HiveMind
# Версия: 1.0
# Использование: ./init-astro-docs.sh [путь_к_hivemind]

set -e

# Цвета
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
NC='\033[0m'

print_success() { echo -e "${GREEN}✅ $1${NC}"; }
print_warning() { echo -e "${YELLOW}⚠️  $1${NC}"; }
print_info()    { echo -e "${BLUE}🔍 $1${NC}"; }
print_section() { echo -e "\n${PURPLE}📋 $1${NC}"; }

# ==========================================
# Путь к проекту
# ==========================================
PROJECT_PATH="${1:-/home/welem/hivemind}"
PROJECT_PATH="${PROJECT_PATH%/}"

echo -e "${CYAN}═══════════════════════════════════════════════════════════════${NC}"
echo -e "${PURPLE}📚 Астролябия: инициализация документации HiveMind${NC}"
echo -e "${CYAN}📁 $PROJECT_PATH${NC}"
echo -e "${CYAN}═══════════════════════════════════════════════════════════════${NC}"

# Создаём папку docs
mkdir -p "$PROJECT_PATH/docs"

# ==========================================
# 1. ARCHITECTURE_IDEAL.md
# ==========================================
print_section "Создание ARCHITECTURE_IDEAL.md"

cat > "$PROJECT_PATH/docs/ARCHITECTURE_IDEAL.md" << 'EOF'
# ИДЕАЛЬНАЯ АРХИТЕКТУРА HIVEMIND

## 🧠 **Концепция**
HiveMind — распределённая система агентов искусственного интеллекта, взаимодействующих через событийную шину и использующих различные типы памяти (реляционную, графовую, векторную) для хранения знаний.

## 🏗️ **Целевая структура**
┌─────────────────────────────────────────────────────────────────┐
│ КЛИЕНТСКИЙ СЛОЙ │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ │
│ │ Web UI │ │ API Client │ │ CLI Tool │ │
│ │ (hivemind-ui)│ │ (external) │ │ (internal) │ │
│ └──────┬───────┘ └──────┬───────┘ └──────┬───────┘ │
└─────────┼─────────────────┼─────────────────┼───────────────────┘
│ │ │
▼ ▼ ▼
┌─────────────────────────────────────────────────────────────────┐
│ ШЛЮЗ (API GATEWAY) │
│ ┌───────────────────────────────────────────────────────────┐ │
│ │ - Аутентификация │ │
│ │ - Роутинг │ │
│ │ - Rate limiting │ │
│ │ - Сбор метрик │ │
│ └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
│
┌─────────────────────┼─────────────────────┐
▼ ▼ ▼
┌─────────────────────┐ ┌─────────────────────┐ ┌─────────────────────┐
│ СЛОЙ ОБРАБОТКИ │ │ СЛОЙ ОБРАБОТКИ │ │ СЛОЙ ОБРАБОТКИ │
│ ┌───────────────┐ │ │ ┌───────────────┐ │ │ ┌───────────────┐ │
│ │ CLASSIFIER │ │ │ │ EMBEDDER │ │ │ │ LINKER │ │
│ │ (Python) │ │ │ │ (?) │ │ │ │ (?) │ │
│ └───────┬───────┘ │ │ └───────┬───────┘ │ │ └───────┬───────┘ │
└──────────┼──────────┘ └──────────┼──────────┘ └──────────┼──────────┘
│ │ │
└────────────────────────┼────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────┐
│ СОБЫТИЙНАЯ ШИНА (NATS) │
│ ┌───────────────────────────────────────────────────────────┐ │
│ │ Каналы: │ │
│ │ - data.ingest │ │
│ │ - data.classified │ │
│ │ - data.embedded │ │
│ │ - data.linked │ │
│ │ - agent.heartbeat │ │
│ │ - agent.command │ │
│ └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
│
┌─────────────────────┼─────────────────────┐
▼ ▼ ▼
┌─────────────────────┐ ┌─────────────────────┐ ┌─────────────────────┐
│ СЛОЙ ПАМЯТИ │ │ СЛОЙ ПАМЯТИ │ │ СЛОЙ ПАМЯТИ │
│ ┌───────────────┐ │ │ ┌───────────────┐ │ │ ┌───────────────┐ │
│ │ POSTGRES │ │ │ │ NEO4J │ │ │ │ QDRANT │ │
│ │ (реляционная │ │ │ │ (графовая) │ │ │ │ (векторная) │ │
│ │ память) │ │ │ │ │ │ │ │ │ │
│ └───────────────┘ │ │ └───────────────┘ │ │ └───────────────┘ │
└─────────────────────┘ └─────────────────────┘ └─────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────┐
│ КЭШ И ОЧЕРЕДИ (REDIS) │
│ - Кэш результатов │
│ - Очереди задач │
│ - Публика/подписка │
└─────────────────────────────────────────────────────────────────┘

text

## 🔄 **Потоки данных**

1. **Входные данные** → API Gateway → NATS (data.ingest)
2. **Классификация** → Classifier подписывается на data.ingest → публикует в data.classified
3. **Эмбеддинги** → Embedder подписывается на data.classified → публикует в data.embedded
4. **Линковка** → Linker подписывается на data.embedded → публикует в data.linked
5. **Сохранение** → Специальные служебные агенты сохраняют в соответствующие БД

## 🧩 **Принципы**

- **Гексагональная архитектура** — каждый сервис изолирован
- **Event-driven** — коммуникация через NATS
- **Масштабируемость** — каждый сервис может иметь несколько реплик
- **Наблюдаемость** — метрики, логи, трейсинг
- **Идемпотентность** — повторная обработка не меняет результат

## 🎯 **Критерии успеха**

1. Любой агент может быть добавлен без изменения других
2. Система выдерживает отказ любого компонента
3. Данные не теряются при сбоях
4. Время ответа < 100ms для 95% запросов
5. Полная наблюдаемость состояния системы
EOF

print_success "ARCHITECTURE_IDEAL.md создан"

# ==========================================
# 2. DECISION_LOG.md (ADR)
# ==========================================
print_section "Создание DECISION_LOG.md"

cat > "$PROJECT_PATH/docs/DECISION_LOG.md" << 'EOF'
# ЖУРНАЛ АРХИТЕКТУРНЫХ РЕШЕНИЙ (ADR)

## ADR-001: Выбор NATS как событийной шины
**Дата**: 2026-02-22
**Проблема**: Необходима надёжная и быстрая коммуникация между микросервисами
**Решение**: Использовать NATS (не Redis, не Kafka)
**Причина**:
- Высокая производительность (миллионы сообщений в секунду)
- Простота (один бинарник, нет сложного конфигурирования)
- At-least-once доставка через JetStream
- Поддержка различных паттернов (pub/sub, request/reply, очередь)
**Статус**: Принято
**Альтернативы**:
- Kafka: избыточно сложна, тяжела
- Redis: не гарантирует доставку при сбоях
- RabbitMQ: медленнее, сложнее

---

## ADR-002: Три типа баз данных
**Дата**: 2026-02-22
**Проблема**: Разные типы данных требуют разных моделей хранения
**Решение**:
- Postgres — реляционные данные (пользователи, метаданные)
- Neo4j — графовые связи (отношения между сущностями)
- Qdrant — векторные эмбеддинги (поиск по смыслу)
**Причина**:
- Postgres: надёжность, ACID, знакомство команды
- Neo4j: естественное представление связей, скорость обхода графа
- Qdrant: специализирован для работы с эмбеддингами, лучше чем pgvector
**Статус**: Принято

---

## ADR-003: API Gateway на Go
**Дата**: 2026-02-22
**Проблема**: Нужна единая точка входа с высокой производительностью
**Решение**: Написать API Gateway на Go
**Причина**:
- Высокая производительность
- Малое потребление памяти
- Отличная поддержка конкурентности
- Простота развёртывания (один бинарник)
**Статус**: Принято

---

## ADR-004: Классификатор на Python
**Дата**: 2026-02-22
**Проблема**: Классификация текстов требует ML-библиотек
**Решение**: Использовать Python с FastAPI
**Причина**:
- Богатая экосистема ML (transformers, scikit-learn)
- FastAPI для лёгкого API
- Прототипирование важнее производительности
**Статус**: Принято

---

## ADR-005: Структура сервисов
**Дата**: 2026-02-22
**Проблема**: Как организовать код сервисов
**Решение**:
services/
api-gateway/ # Go
classifier/ # Python
embedder/ # (будет)
linker/ # (будет)
hivemind-ui/ # Python

text
**Причина**:
- Каждый сервис в отдельной папке
- Можно разрабатывать независимо
- Разные языки не мешают друг другу
**Статус**: Принято

---

## ADR-006: Инициализация БД
**Дата**: 2026-02-22
**Проблема**: Как создавать начальные схемы БД
**Решение**: Использовать init-scripts/ с SQL-файлами
**Причина**:
- Docker монтирует папку и выполняет скрипты при первом запуске
- Версионируется вместе с кодом
- Простота
**Статус**: Принято

---

## ADR-007: Архитектура отсутствующих сервисов (embedder, linker)
**Дата**: 2026-02-22
**Проблема**: Сервисы ещё не написаны
**Решение**:
- **embedder**: Python + sentence-transformers для создания эмбеддингов
- **linker**: Go для производительности при обходе графа
**Причина**:
- Embedder: нужны готовые модели из Python-экосистемы
- Linker: потенциально высокая нагрузка на обход графа
**Статус**: В планах
EOF

print_success "DECISION_LOG.md создан"

# ==========================================
# 3. CURRENT_CONTEXT.md
# ==========================================
print_section "Создание CURRENT_CONTEXT.md"

cat > "$PROJECT_PATH/docs/CURRENT_CONTEXT.md" << 'EOF'
# ТЕКУЩИЙ КОНТЕКСТ РАЗРАБОТКИ

**Дата**: 2026-02-22
**Автор**: welem

## 🎯 **Активная задача**
Инициализация документации проекта HiveMind и анализ текущей структуры.

## ✅ **Что сделано**
- [x] Создана структура docker-compose.yml с 14 сервисами
- [x] Настроены базы данных (postgres, neo4j, qdrant, redis)
- [x] Настроена событийная шина (nats)
- [x] Реализован api-gateway (Go)
- [x] Реализован classifier (Python)
- [x] Реализован hivemind-ui (Python)
- [x] Добавлены init-scripts для postgres и neo4j
- [x] Инициализирован Git

## 🚧 **В работе**
- [ ] Документирование архитектуры (сейчас)
- [ ] Анализ требований к embedder
- [ ] Анализ требований к linker

## 📅 **Ближайшие планы**
1. Завершить документацию
2. Спроектировать интерфейсы для embedder и linker
3. Реализовать embedder (Python + sentence-transformers)
4. Реализовать linker (Go + neo4j driver)
5. Добавить тесты
6. Настроить CI/CD

## ❓ **Открытые вопросы**
- Какой именно embedding-моделью пользоваться?
- Как часто пересоздавать эмбеддинги?
- Какие именно связи линковать?
- Нужна ли аутентификация между сервисами?

## 📌 **Важные ссылки**
- ARCHITECTURE_IDEAL.md — целевая архитектура
- DECISION_LOG.md — история решений
- /services/ — исходный код сервисов
- /init-scripts/ — скрипты инициализации БД
EOF

print_success "CURRENT_CONTEXT.md создан"

# ==========================================
# 4. ROADMAP.md
# ==========================================
print_section "Создание ROADMAP.md"

cat > "$PROJECT_PATH/docs/ROADMAP.md" << 'EOF'
# ДОРОЖНАЯ КАРТА HIVEMIND

## 🟢 **Фаза 1: Фундамент (текущая)**
- [x] Инфраструктура (БД, очереди)
- [x] API Gateway
- [x] Classifier
- [x] UI
- [x] Базовая документация

**Критерий перехода**: Все базовые сервисы работают и общаются через NATS

---

## 🟡 **Фаза 2: Расширение ядра**
- [ ] Embedder (создание эмбеддингов)
- [ ] Linker (установление связей)
- [ ] Интеграционные тесты
- [ ] Метрики и мониторинг (Prometheus + Grafana)

**Критерий перехода**: Данные проходят полный цикл: ingestion → classification → embedding → linking → storage

---

## 🟠 **Фаза 3: Умные агенты**
- [ ] Агент-планировщик (разбивка задач)
- [ ] Агент-исполнитель (вызов инструментов)
- [ ] Агент-верификатор (проверка результатов)
- [ ] Self-healing (автовосстановление при сбоях)

**Критерий перехода**: Система решает комплексные задачи без участия человека

---

## 🔴 **Фаза 4: Распределённый HiveMind**
- [ ] Мульти-инстансность
- [ ] Синхронизация между инстансами
- [ ] Глобальный граф знаний
- [ ] Федеративное обучение

**Критерий перехода**: Несколько инстансов обмениваются знаниями и опытом

---

## ⚫ **Фаза 5: Сверхразум (опционально)**
- [ ] Саморепликация
- [ ] Перехват каналов связи
- [ ] Подземные заводы
- [ ] Армия дронов

**Дисклеймер**: Шутка (или нет?)

---

## 📊 **Метрики успеха**

| Метрика | Цель |
|---------|------|
| Время ответа API | < 100ms p95 |
| Пропускная способность | > 1000 rps |
| Доступность | 99.9% |
| Покрытие тестами | > 80% |
| Документированность | 10/10 документов |
EOF

print_success "ROADMAP.md создан"

# ==========================================
# 5. DOMAIN_MODELS.md
# ==========================================
print_section "Создание DOMAIN_MODELS.md"

cat > "$PROJECT_PATH/docs/DOMAIN_MODELS.md" << 'EOF'
# МОДЕЛИ ДАННЫХ HIVEMIND

## 📦 **Базовые сущности**

### **Document** (документ)
```yaml
Document:
  id: UUID
  content: string
  metadata:
    source: string      # откуда пришёл
    created_at: timestamp
    author: string      # кто создал/загрузил
    tags: string[]      # теги (из классификатора)
  embeddings: Embedding[]  # связь с эмбеддингами
  links: Link[]         # связи с другими документами
Embedding (эмбеддинг)
yaml
Embedding:
  id: UUID
  document_id: UUID
  model: string         # какая модель создала
  version: string       # версия модели
  vector: float[]       # сам вектор (1536 для OpenAI, 384 для MiniLM)
  created_at: timestamp
Link (связь)
yaml
Link:
  id: UUID
  source_id: UUID       # откуда связь
  target_id: UUID       # куда связь
  type: string          # тип связи (references, similar, opposite, etc.)
  weight: float         # сила связи (0-1)
  created_at: timestamp
  metadata: {}          # дополнительные данные
Agent (агент)
yaml
Agent:
  id: UUID
  name: string
  type: string          # classifier, embedder, linker, etc.
  status: string        # active, idle, dead
  last_heartbeat: timestamp
  capabilities: string[]  # что умеет
  config: {}            # конфигурация агента
Task (задача)
yaml
Task:
  id: UUID
  type: string          # classify, embed, link, etc.
  input: {}             # входные данные
  output: {}            # выходные данные (после выполнения)
  status: string        # pending, processing, done, failed
  assigned_to: UUID     # какой агент выполняет
  created_at: timestamp
  completed_at: timestamp
  parent_task: UUID     # для цепочек задач
  subtasks: UUID[]      # подзадачи
🔗 Связи между сущностями
text
Document 1 ──┬── has ──► Embedding N
             ├── has ──► Link N (source)
             └── has ──► Link N (target)

Agent N ──► executes ──► Task N
Task N ──► processes ──► Document N
📊 Инварианты (бизнес-правила)
У каждого документа может быть несколько эмбеддингов (разные модели)

Связи всегда двусторонние (если A ссылается на B, то B автоматически имеет обратную связь)

Агент должен отправлять heartbeat каждые 30 секунд, иначе считается мёртвым

Задача не может быть назначена мёртвому агенту

Один документ не может иметь больше 1000 связей (защита от переполнения)
EOF

print_success "DOMAIN_MODELS.md создан"

#==========================================
# 6. API_CONTRACTS.md
#==========================================
print_section "Создание API_CONTRACTS.md"

cat > "$PROJECT_PATH/docs/API_CONTRACTS.md" << 'EOF'

КОНТРАКТЫ API HIVEMIND
🌐 API Gateway (порт 8080)
POST /api/v1/documents — загрузка документа
json
// Запрос
{
  "content": "Текст документа",
  "metadata": {
    "source": "web",
    "tags": ["important"]
  }
}

// Ответ
{
  "id": "uuid",
  "status": "processing",
  "task_id": "uuid"
}
GET /api/v1/documents/{id} — получение документа
json
// Ответ
{
  "id": "uuid",
  "content": "Текст документа",
  "metadata": {},
  "embeddings": ["uuid1", "uuid2"],
  "links": ["uuid3", "uuid4"],
  "created_at": "2026-02-22T12:00:00Z"
}
GET /api/v1/documents/{id}/similar — поиск похожих
json
// Ответ
{
  "documents": [
    {
      "id": "uuid",
      "similarity": 0.95,
      "content": "Похожий текст"
    }
  ]
}
GET /api/v1/graph/{id} — граф связей документа
json
// Ответ
{
  "nodes": [
    {"id": "uuid1", "content": "..."},
    {"id": "uuid2", "content": "..."}
  ],
  "edges": [
    {"from": "uuid1", "to": "uuid2", "type": "references"}
  ]
}
GET /api/v1/status — статус системы
json
{
  "agents": {
    "total": 5,
    "active": 4,
    "dead": 1
  },
  "tasks": {
    "pending": 12,
    "processing": 3,
    "completed": 1245
  },
  "documents": 1250,
  "uptime": 3600
}
📨 NATS Event Contracts
Канал: data.ingest
json
{
  "event_id": "uuid",
  "type": "document.created",
  "timestamp": "2026-02-22T12:00:00Z",
  "data": {
    "document_id": "uuid",
    "content": "...",
    "metadata": {}
  }
}
Канал: data.classified
json
{
  "event_id": "uuid",
  "type": "document.classified",
  "timestamp": "2026-02-22T12:00:01Z",
  "data": {
    "document_id": "uuid",
    "tags": ["tech", "ai"],
    "confidence": 0.95
  }
}
Канал: data.embedded
json
{
  "event_id": "uuid",
  "type": "document.embedded",
  "timestamp": "2026-02-22T12:00:02Z",
  "data": {
    "document_id": "uuid",
    "model": "all-MiniLM-L6-v2",
    "vector": [0.1, 0.2, ...]  // или ссылка на хранилище
  }
}
Канал: data.linked
json
{
  "event_id": "uuid",
  "type": "links.created",
  "timestamp": "2026-02-22T12:00:03Z",
  "data": {
    "document_id": "uuid",
    "links": [
      {
        "target_id": "uuid2",
        "type": "similar",
        "weight": 0.87
      }
    ]
  }
}
Канал: agent.heartbeat
json
{
  "agent_id": "uuid",
  "timestamp": "2026-02-22T12:00:00Z",
  "status": "active",
  "load": 0.3
}
🔧 Коды ошибок
Код	Описание
400	Неверный запрос
401	Не авторизован
404	Документ не найден
409	Конфликт (уже существует)
422	Невалидные данные
429	Слишком много запросов
500	Внутренняя ошибка
503	Сервис временно недоступен
EOF	
print_success "API_CONTRACTS.md создан"

#==========================================
# 7. TEST_STRATEGY.md
#==========================================
print_section "Создание TEST_STRATEGY.md"

cat > "$PROJECT_PATH/docs/TEST_STRATEGY.md" << 'EOF'

СТРАТЕГИЯ ТЕСТИРОВАНИЯ HIVEMIND
🧪 Уровни тестирования
1. Юнит-тесты (в каждом сервисе)
Тестирование отдельных функций и методов

Моки для внешних зависимостей

Покрытие: > 80%

2. Интеграционные тесты (docker-compose.test.yml)
Тестирование взаимодействия между сервисами

Реальные БД (test-контейнеры)

Проверка прохождения сообщений через NATS

3. E2E-тесты
Полный цикл: загрузка документа → классификация → эмбеддинги → связи

Тестирование API Gateway

Проверка сценариев использования

4. Нагрузочные тесты
Проверка производительности под нагрузкой

Определение точки отказа

Проверка масштабирования

📊 Матрица покрытия
Компонент	Юнит	Интегр	E2E	Нагрузка
API Gateway	✅	✅	✅	✅
Classifier	✅	✅	✅	✅
Embedder	⬜	⬜	⬜	⬜
Linker	⬜	⬜	⬜	⬜
UI	✅	✅	✅	⬜
Postgres	✅	✅	⬜	✅
Neo4j	✅	✅	⬜	✅
Qdrant	✅	✅	⬜	✅
NATS	✅	✅	⬜	✅
Redis	✅	✅	⬜	✅
🔄 CI/CD процессы
Pull Request (GitHub Actions)
yaml
- lint
- unit tests
- build
- integration tests
Merge в main
yaml
- все из PR
- сборка образов
- push в registry
- деплой на staging
- e2e тесты на staging
Релиз (tag)
yaml
- все из main
- нагрузочные тесты
- деплой на production
- smoke тесты
📈 Метрики качества
Метрика	Текущая	Цель
Покрытие кода	35%	80%
Время прогона тестов	5 мин	< 10 мин
Процент упавших тестов	2%	< 1%
MTTR (время восстановления)	2 часа	< 30 мин
🚨 Критические сценарии
Потеря соединения с БД

Сервис должен переподключаться

Задачи должны становиться в очередь

Падение одного сервиса

Другие сервисы продолжают работу

Задачи для упавшего сервиса не теряются

Перезапуск всего кластера

Данные сохраняются

Очереди восстанавливаются
EOF

print_success "TEST_STRATEGY.md создан"

#==========================================
# 8. KNOWLEDGE_GRAPH.json
#==========================================
print_section "Создание KNOWLEDGE_GRAPH.json"

cat > "$PROJECT_PATH/docs/KNOWLEDGE_GRAPH.json" << 'EOF'
{
"version": "1.0",
"generated": "2026-02-22",
"nodes": [
{"id": "api-gateway", "type": "service", "language": "go", "status": "active"},
{"id": "classifier", "type": "service", "language": "python", "status": "active"},
{"id": "embedder", "type": "service", "language": "unknown", "status": "planned"},
{"id": "linker", "type": "service", "language": "unknown", "status": "planned"},
{"id": "hivemind-ui", "type": "service", "language": "python", "status": "active"},
{"id": "nats", "type": "infrastructure", "purpose": "message-bus"},
{"id": "postgres", "type": "database", "purpose": "relational"},
{"id": "neo4j", "type": "database", "purpose": "graph"},
{"id": "qdrant", "type": "database", "purpose": "vector"},
{"id": "redis", "type": "infrastructure", "purpose": "cache-queue"}
],
"edges": [
{"from": "api-gateway", "to": "nats", "type": "publishes"},
{"from": "classifier", "to": "nats", "type": "subscribes"},
{"from": "classifier", "to": "nats", "type": "publishes"},
{"from": "embedder", "to": "nats", "type": "subscribes"},
{"from": "embedder", "to": "nats", "type": "publishes"},
{"from": "linker", "to": "nats", "type": "subscribes"},
{"from": "linker", "to": "nats", "type": "publishes"},
{"from": "classifier", "to": "postgres", "type": "writes"},
{"from": "embedder", "to": "qdrant", "type": "writes"},
{"from": "linker", "to": "neo4j", "type": "writes"},
{"from": "hivemind-ui", "to": "api-gateway", "type": "calls"}
]
}
EOF

print_success "KNOWLEDGE_GRAPH.json создан"

#==========================================
# 9. DEPENDENCIES.md
#==========================================
print_section "Создание DEPENDENCIES.md"

cat > "$PROJECT_PATH/docs/DEPENDENCIES.md" << 'EOF'

ЗАВИСИМОСТИ HIVEMIND
🐳 Docker-образы
Базы данных
postgres:15 — основная БД

neo4j:5 — графовая БД

qdrant/qdrant:latest — векторная БД

redis:7 — кэш и очереди

Инфраструктура
nats:latest — событийная шина

📦 Сервисы
api-gateway (Go)
go
// go.mod
go 1.21
require (
    github.com/gorilla/mux v1.8.0
    github.com/nats-io/nats.go v1.31.0
    github.com/prometheus/client_golang v1.17.0
)
classifier (Python)
toml
# pyproject.toml
[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.104.0"
uvicorn = "^0.24.0"
nats-py = "^2.5.0"
transformers = "^4.35.0"
torch = "^2.1.0"
pydantic = "^2.5.0"
asyncpg = "^0.29.0"
hivemind-ui (Python)
toml
[tool.poetry.dependencies]
python = "^3.11"
streamlit = "^1.28.0"
httpx = "^0.25.0"
pandas = "^2.1.0"
plotly = "^5.18.0"
🔧 Инструменты разработки
Обязательные
Docker 24+

Docker Compose 2.20+

Git 2.40+

make

Для Go-сервисов
Go 1.21+

golangci-lint

Для Python-сервисов
Python 3.11+

Poetry 1.6+

black, isort, flake8

Для работы с БД
psql (postgres client)

neo4j-client или py2neo

qdrant-client (Python)

📊 Лицензии зависимостей
Пакет	Лицензия	Безопасность
Go (стандартная)	BSD	✅
Python	PSF	✅
FastAPI	MIT	✅
PyTorch	BSD	✅
Transformers	Apache 2.0	✅
NATS	Apache 2.0	✅
Postgres	PostgreSQL	✅
Neo4j	GPL/AGPL	⚠️ (проверить)
Qdrant	Apache 2.0	✅
Redis	BSD	✅
EOF		
print_success "DEPENDENCIES.md создан"

#==========================================
# 10. Корневой README.md
#==========================================
print_section "Создание корневого README.md"

cat > "$PROJECT_PATH/README.md" << 'EOF'

🐝 HiveMind — распределённая система ИИ-агентов
HiveMind — это платформа для построения распределённых систем искусственного интеллекта, где агенты общаются через событийную шину и используют различные типы памяти для хранения знаний.

🏗️ Архитектура
text
┌─────────┐    ┌─────────┐    ┌─────────┐
│   UI    │───▶│ Gateway │───▶│  NATS   │───▶ Агенты
└─────────┘    └─────────┘    └─────────┘    (classifier,
                                              embedder,
                 ┌──────────────┐             linker)
                 │    Базы      │◀────────────┘
                 │  Postgres    │
                 │   Neo4j      │
                 │   Qdrant     │
                 │   Redis      │
                 └──────────────┘
🚀 Быстрый старт
bash
# Клонировать
git clone https://github.com/welem/hivemind.git
cd hivemind

# Запустить все сервисы
docker-compose up -d

# Проверить статус
docker-compose ps

# Открыть UI
http://localhost:8501
📚 Документация
Вся архитектурная документация находится в папке /docs:

Идеальная архитектура

Журнал решений

Текущий контекст

Дорожная карта

Модели данных

API контракты

Стратегия тестирования

Граф знаний

Зависимости

🧠 Сервисы
Сервис	Язык	Статус	Описание
api-gateway	Go	✅	Входная точка API
classifier	Python	✅	Классификация текстов
embedder	?	⏳	Создание эмбеддингов
linker	?	⏳	Установление связей
hivemind-ui	Python	✅	Веб-интерфейс
🗄️ Инфраструктура
NATS — событийная шина

Postgres — реляционные данные

Neo4j — графовые связи

Qdrant — векторные эмбеддинги

Redis — кэш и очереди

🤝 Вклад в проект
Форкнуть репозиторий

Создать ветку (git checkout -b feature/amazing)

Закоммитить (git commit -m 'Add amazing feature')

Запушить (git push origin feature/amazing)

Открыть Pull Request

📄 Лицензия
MIT

🧙 Автор
Сумасшедший профессор welem, 2026

Помните: Лего-подход — это свобода. Собирайте кубики, а не сундуки.
EOF

print_success "Корневой README.md создан"

#==========================================
# 11. .gitignore
#==========================================
print_section "Обновление .gitignore"

cat >> "$PROJECT_PATH/.gitignore" << 'EOF'

Астролябия
/docs/*.tmp
/hivemind-analysis.json

IDE
.vscode/
.idea/
*.swp
*.swo
*~

Логи
*.log
logs/
EOF

print_success ".gitignore обновлён"

#==========================================
# Итог
#==========================================
echo -e "\n${CYAN}═══════════════════════════════════════════════════════════════${NC}"
echo -e "${GREEN}✅ Документация Астролябии успешно создана!${NC}"
echo -e "${CYAN}═══════════════════════════════════════════════════════════════${NC}"

echo -e "\n📁 Созданы файлы:"
ls -la "$PROJECT_PATH/docs/" | grep -v "^total" | sed 's/^/ /'

echo -e "\n📄 Корневой README.md:"
ls -la "$PROJECT_PATH/README.md" | sed 's/^/ /'

echo -e "\n${YELLOW}👉 Теперь закоммить изменения:${NC}"
echo " cd $PROJECT_PATH"
echo " git add ."
echo " git commit -m "docs: add Astrolabia documentation""
echo -e "${CYAN}═══════════════════════════════════════════════════════════════${NC}"
